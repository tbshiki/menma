# セキュリティポリシー

## 対応しているバージョン

`main` の最新の内容だけを対象とします。過去のタグへの修正の取り込みは行っていません。

## 脆弱性を見つけたら

**公開の Issue や Pull Request では報告しないでください。** GitHub の非公開の報告窓口を使ってください。

1. <https://github.com/tbshiki/menma/security/advisories> を開く
2. **Report a vulnerability** から報告する

次の情報があると確認が早くなります。

- 再現手順（できれば最小の原稿や操作）
- 影響（何ができてしまうか）
- 試したブラウザとそのバージョン

返信までの目安は 1 週間です。修正した内容は Security Advisory として公開します。

## このプロジェクトで特に見てほしい範囲

menma はブラウザだけで動く静的なスライド表示ツールです。サーバ処理も、利用者のデータを外部へ送る経路も持ちません。そのうえで、次の性質が壊れていたら脆弱性として扱います。

- **原稿からのスクリプト実行**: 原稿の Markdown は raw HTML を無効にして解釈し、`innerHTML` へ渡すのは Markdown パーサの出力に限っています。任意のスクリプトが動く経路があれば報告してください
- **読み込んだ内容の外部送信**: 読み込んだ原稿と画像はブラウザの中（IndexedDB）に留まります。外部へ出る経路があれば報告してください
- **配信時のヘッダ**: CSP などを `public/_headers` で付けています。これを迂回できる経路も対象です

設計上の方針は [設計 12 章](docs/architecture.md)、配信時のヘッダの根拠は [決定記録 D-25](docs/decisions.md) にあります。

## 対象外

- 依存パッケージそのものの脆弱性は、まず提供元へ報告してください（このリポジトリでは Dependabot で追跡しています）
- 利用者が自分で書いた原稿の内容に起因する表示崩れ
